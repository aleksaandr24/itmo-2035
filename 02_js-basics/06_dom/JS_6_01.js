"use strict";

let first = document.getElementById("first");
let second = document.getElementById("second");

console.log(first.innerHTML); // Выведет DOM
console.log(first.innerText); // Выведет DOM
console.log(second.innerHTML); // Выведет DOM I
console.log(second.innerText); // Выведет DOM I